<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.epicodus.androidapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/weather"
        android:label="Weather"
        android:roundIcon="@mipmap/weather"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">

        </activity>
        <activity android:name=".ui.LocalActivity" />
        <activity android:name=".ui.LocalListActivity" />
        <activity android:name=".ui.SavedLocationsActivity" />
        <activity android:name=".ui.LoginActivity" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
        <activity android:name=".ui.AccountCreateActivity"></activity>
        <activity android:name=".ui.LocalDetailActivity"></activity>
    </application>

</manifest>